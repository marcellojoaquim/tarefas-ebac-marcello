server:
  port: 8081
spring:
  application:
    name: demo-mongodb
  data:
    mongodb:
      uri: "mongodb+srv://${MONGO_USER}:${MONGO_PASS}@clusterebac.augbryg.mongodb.net/?appName=ClusterEbac"
      database: ebac